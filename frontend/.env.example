# Stream (Chat & Video) API Key
# Must be the SAME key as STEAM_API_KEY in backend/.env
# Get this from https://getstream.io/ dashboard after creating an app
VITE_STREAM_API_KEY=your_stream_api_key_here